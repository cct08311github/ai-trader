repos:
  - path: "."
    name: ai-trader
    language: python
stale_threshold_days: 180
max_issues_per_run: 5
exclude_patterns:
  - node_modules
  - .venv
  - bin/venv
  - deploy-offline
  - __pycache__
  - .next
  - .git
