fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
pytest>=8.0
hypothesis>=6.0
httpx>=0.27
psutil>=5.9

# SSE
sse-starlette>=2.1

# Google Gemini (for daily PM review)
google-genai>=1.0

# Anthropic Claude (for chat feature — optional, falls back to Gemini)
anthropic>=0.84

requests>=2.28
certifi>=2024.0.0

# Optional: install separately for live/simulation broker mode
# pip install shioaji>=1.0
