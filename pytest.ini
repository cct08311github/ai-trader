[pytest]
# Only run the project tests. Reference packages/docs are not part of CI.
testpaths =
    tests
    src/tests
    tools/self_evolution/tests
pythonpath =
    src
    frontend/backend
    .
norecursedirs =
    ref_doc
    ref_package
    scripts
addopts = -q
