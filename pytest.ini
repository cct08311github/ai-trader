[pytest]
# Only run the project tests. Reference packages/docs are not part of CI.
testpaths =
    tests
pythonpath =
    src
norecursedirs =
    ref_doc
    ref_package
    src/tests
    scripts
addopts = -q
